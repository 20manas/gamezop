version: '3.8'
services:
  postgres:
    volumes:
      - postgresvolume:/var/lib/postgresql/data
  node:
    environment:
      - NODE_ENV=production
    command: npm start
volumes:
  postgresvolume:
